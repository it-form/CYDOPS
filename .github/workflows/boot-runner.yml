name: Reboot Runner
on:
  push:
    branches:
      - LABOS
jobs:
  test-runner:
    runs-on:
      - self-hosted
      - linux
      - x64

    steps:
       - run: |
           echo "$(date '+%Y-%m-%d %H:%M:%S') - Reboot effectué par: $(whoami)" > /home/user/message
           sudo reboot
